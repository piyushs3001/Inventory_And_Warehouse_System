/**
 * Generated by orval v8.15.0 🍺
 * Do not edit manually.
 * Inventory & Warehouse System API
 * REST API for the Inventory & Warehouse System
 * OpenAPI spec version: 1.0
 */

export interface InventoryItemDto {
  /** Inventory item id (UUID). */
  id: string;
  /** Product id (UUID). */
  productId: string;
  /** Product name. */
  productName: string;
  /** Product SKU. */
  sku: string;
  /** Warehouse id (UUID). */
  warehouseId: string;
  /** Warehouse name. */
  warehouseName: string;
  /** Available units (sellable, on hand). */
  available: number;
  /** Reserved units (allocated, still in warehouse). */
  reserved: number;
  /** Damaged units (unsellable). */
  damaged: number;
  /** In-transit units (moving between warehouses). */
  inTransit: number;
  /** Total on hand = available + reserved + damaged + inTransit. */
  total: number;
  /** Reorder threshold for this product. */
  reorderLevel: number;
  /** True when available is at or below the reorder level (server-computed). */
  lowStock: boolean;
  /** Last update timestamp. */
  updatedAt: string;
}
