// Prisma schema — see https://pris.ly/d/prisma-schema
// pgvector is deferred to the AI phase; reintroduce postgresqlExtensions + extensions=[vector] there.

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
